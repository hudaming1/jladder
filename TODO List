【OK】不能访问http://news.cssn.cn/images/mobile_v2020.js   【奇怪，用\n替换\r\n就可以了】

【P0】就是 workerCount的问题，设置成1个会稳，但是会慢，且阻塞时会导致全部请求block住；如果设置多个，就会相互串道，导致请求无响应返回，或收到rst包。

【P0】HTTP基本稳了，HTTPS个别串道，用百度首页就能复现
【P0】下载时Outside端还是会OutOfDirectMemory
【P0】由于worker数量=1，因此多任务并行时会互相影响
【P1】inside-outside的重连机制
【P2】inside和outside连接相互隔离，避免一个连接抛出异常，整个线程池crash掉
【P3】暂时先用\N吧


测试网址：
https://tieba.baidu.com/p/1934517275#!/l/p1
http://static.bshare.cn/frame/images/logos/mp2/sprite/top_logos_sprite.gif



1.不能使用worker-pool；共用一个worker处理没有问题 or 为每个请求单独创建一个worker发生错误概率明显会减少
2.inside的write和outisde的receive发现对不上
3.出现问题的请求大部分隶属于同一个域名，例如百度首页的 dss0.baidu.com
4.目前发现只有HTTPS有这个问题，HTTP的目前都正常